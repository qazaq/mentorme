<nav class="navbar navbar-default">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">MentorMe</a>
    <ul class="nav navbar-nav navbar-right">
      <li><%= link_to "Write a story", new_post_path , class: "button" %></li>
      <% if user_signed_in? %>
        <li><a href="#" class="button"><%= current_user.email %></a></li>
        <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "button" %></li>
      <% else %>
        <li><%= link_to "Sign In/ Sign Up", new_user_session_path, class: "button" %></li>
      <% end %>
  </div>
</nav>
